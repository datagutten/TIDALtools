language: php

matrix:
  fast_finish: true

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - nightly

allow_failures:
  - php: nightly

install:
 - composer install --dev --no-interaction

before_script:
 - phpunit --version

script:
 - phpunit --bootstrap vendor/autoload.php tests
